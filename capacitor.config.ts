import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-app-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
